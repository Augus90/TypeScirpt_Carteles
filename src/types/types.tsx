export type agenciaType = {
    id: number,
    remitente: string,
    destinatario: string,
    ciudad: string,
    direccion: string,
    retira: string,
    observaciones: string,
  }

export type listaDeAgenciasType = {
    id: number,
    agencia : string,
    valor : string,
  }